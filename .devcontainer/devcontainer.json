{
  "name": "Python Tkinter GUI Environment",
  "image": "mcr.microsoft.com/devcontainers/base:bookworm",
  
  // Automates installing the VNC server and light desktop interface
  "features": {
    "ghcr.io/devcontainers/features/desktop-lite:1": {
      "password": "vscode",
      "webPort": "6080",
      "vncPort": "5901"
    }
  },

  // Auto-installs Tkinter into the Linux container right after creation
  "postCreateCommand": "if command -v apt >/dev/null 2>&1; then sudo apt update && sudo apt install -y python3 python3-pip python3-tk; elif command -v apk >/dev/null 2>&1; then sudo apk add --no-cache python3 py3-pip python3-tkinter; fi",

  // Explicitly forwards the web browser noVNC port to your machine
  "forwardPorts": [6080],

  "portsAttributes": {
    "6080": {
      "label": "noVNC Desktop",
      "onAutoForward": "notify"
    }
  }
}